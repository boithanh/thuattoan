{
  "name": "any-random",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx esbuild ts/* --bundle --outdir=js --platform=browser --watch",
    "sync": "node scripts/build.js",
    "build": "npx esbuild ts/* --bundle --outdir=obfuscated/js --platform=browser --minify && npx javascript-obfuscator obfuscated/js --output obfuscated/js && npm run sync"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "fs-extra": "^11.3.0",
    "javascript-obfuscator": "^4.1.1",
    "typescript": "^5.8.3"
  }
}