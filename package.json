{
  "name": "any-random",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx esbuild ts/anyrandom.ts ts/homepage.ts --bundle --outdir=js --platform=browser --watch",
    "build": "npx esbuild ts/anyrandom.ts ts/homepage.ts --bundle --outdir=dist --platform=browser --minify && npx javascript-obfuscator dist --output dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "javascript-obfuscator": "^4.1.1",
    "typescript": "^5.8.3"
  }
}